# UI/UX 검증 보고서

대상: `vehicle-log-app` (작업 트리, master `b2ff061`)
일시: 2026-08-11
방법: `node tests/visual_audit.js` — 뷰포트 12종에서 스크린샷 12장 + 자동 측정

## 어떻게 검증했나

계획서는 스크린샷을 눈으로 보는 방식이었다. 그대로 하면 **접힌 화면 아래의 결함을
놓친다.** 실제로 이번에 찾은 가장 큰 문제는 화면에 아예 보이지 않는 요소였다.
그래서 스크린샷은 그대로 찍되, 판정은 계산으로 한다.

| 측정 항목 | 기준 | 눈으로 안 되는 이유 |
|---|---|---|
| 가로 넘침 | 뷰포트 밖으로 나간 요소, 페이지 `scrollWidth` | 스크롤해야 보이거나 투명해서 안 보임 |
| 명도 대비 | WCAG 2.2 AA — 본문 4.5:1, 큰 글자 3:1 | 화면 밝기·눈에 따라 판단이 갈림 |
| 터치 타깃 | 24×24 CSS px | 화면에서 크기를 잴 수 없음 |
| 거부 동작 | `aria-invalid` 표시 + 포커스 + 뷰포트 진입 | 정적 이미지로는 확인 불가 |

실행 환경도 계획서에서 두 가지 줄였다. `puppeteer` 대신 **`puppeteer-core` + 이미
설치된 크롬**을 써서 크로미움 150MB 내려받기를 없앴고, `npx serve` 대신 **`node:http`
20줄**로 파일 4개를 서빙한다. 의존성은 `puppeteer-core` 하나뿐이다.

**감사는 실제 스프레드시트에 절대 쓰지 않는다.** Apps Script로 나가는 요청은 전부
가로채 가짜 JSON을 돌려준다. 12행 누적 시나리오가 없었다면 실데이터에 쓰레기 행이
열두 개 쌓였을 것이다.

## 검증한 시나리오

| # | 시나리오 | 뷰포트 | 결과 |
|---|---|---|---|
| 1 | 데스크톱 라이트 | 1920×1080 | 통과 |
| 2 | 데스크톱 다크 | 1920×1080 | 통과 |
| 3 | iPhone SE | 375×667 | 통과 |
| 4 | iPhone 14 Pro Max | 430×932 | 통과 |
| 5 | iPad Mini | 768×1024 | **결함 발견 → 수정** |
| 6 | Galaxy Z Fold (접힘) | 280×653 | 통과 |
| 7 | 폼 입력 중 | 375×667 | 통과 |
| 8 | 유효성 오류 | 375×667 | **결함 발견 → 수정** |
| 9 | 기록 12건 (PC) | 1920×1080 | 통과 |
| 10 | 기록 12건 (모바일) | 375×667 | 통과 |
| 11 | 긴 값 + 최소 폭 | 320×568 | 통과 |
| 12 | 2단 분할 경계 | 1100×900 | **결함 발견 → 수정** |

계획서의 10개에 11·12를 더했다. 좌우로 새기 가장 쉬운 두 상태 — 최소 폭에서 긴
주소가 들어올 때와, 2단 레이아웃이 1단으로 접히기 직전 폭 — 가 빠져 있었다.
실제로 12번에서 결함이 나왔다.

## 발견한 결함

### 1. 페이지가 가로로 밀림 (768px·1100px) — 수정

`.sr-only`가 `position: absolute`인데 `.table-wrapper` 안에 위치 기준 조상이 없었다.
컨테이닝 블록이 스크롤 컨테이너 **위**에 잡히면서 `overflow-x: auto`의 클리핑을
빠져나가, 페이지 전체를 뷰포트보다 135px 넓게 늘렸다.

표를 의심하기 쉬운 자리였는데, 각 층의 폭을 실측하니 표는 692px 컨테이너 안에
902px로 **제대로 갇혀 있었다.** 범인은 화면에 보이지도 않는 스크린리더용 텍스트였다.
정상 흐름의 1px 요소로 바꿔 해결했다.

### 2. 거부된 제출이 어느 칸인지 알려주지 않음 — 수정

검증 분기 10개가 전부 메시지만 띄우고 끝났다. 경고는 폼 맨 아래 저장 버튼 위에
뜨는데, 폰에서 이 폼은 세 화면 높이다. 맨 위 '차량'이 문제여도 운전자는 메시지를
읽고 되짚어 올라가며 열한 개 입력칸 중 어느 것인지 찾아야 했다.

10군데를 각각 고치는 대신 필드를 함께 받는 `reject()` 하나로 묶어, 표시·포커스·
스크롤을 한곳에서 처리한다. 잘못된 칸은 테두리와 배경으로도 표시된다.

### 3. 흐린 글자 대비가 기준선에 걸침 — 수정

`--ink-3`가 흰 배경에서 **4.51:1**. AA 기준 4.5에 1/100 차이였다. 이 토큰 하나가
힌트·단위·플레이스홀더·부제를 전부 칠하므로, 표면 색을 조금만 건드려도 그 전부가
동시에 미달로 떨어지는 상태였다. `#666662`로 조여 5.23:1을 확보했다.

## 확인했지만 문제가 아니었던 것

- **"삭제" 버튼이 흐리다** — 눈으로는 대비 미달로 보였으나 측정값 5.01:1로 기준
  통과. 크기도 24×24 이상. 눈이 틀렸고 측정이 맞았다.
- **표가 좌우로 스크롤된다** — `.table-wrapper`의 의도된 설계다. 620px 아래에서는
  컨테이너 쿼리로 label:value 카드로 바뀐다.
- **숫자 입력창이 다른 UI를 침범한다** (최초 지적 사항) — 작업 트리에서는 이미
  해결돼 있다. 계기판·운행거리·인원 모두 열 안에 들어가고 단위가 입력창 안쪽에
  붙는다. **라이브에 배포되지 않았을 뿐이다.**

## 현재 상태

12/12 통과. 대비 위반 0건, 터치 타깃 위반 0건.

## 남은 일

**배포되지 않았다.** 위 수정은 전부 로컬 커밋 상태이고 `origin`에 push하지 않았다.
Cloudflare Pages는 GitHub를 보고 빌드하므로, push하기 전까지 라이브 사이트는 8/7
버전 그대로다 — 최초 지적한 숫자 입력창 문제도 라이브에는 남아 있다.

`a31c6bd`에는 8/7부터 미커밋 상태로 남아 있던 프론트엔드 재설계가 함께 들어갔다.
검토 후 push 여부를 결정해야 한다.

## 재현

```
npm install puppeteer-core --no-save
node tests/visual_audit.js              # 판정
AUDIT_VERBOSE=1 node tests/visual_audit.js   # 측정 근거까지
```

스크린샷은 `tests/screenshots/`에 생성된다(gitignore 대상).
